#include<bits/stdc++.h>
using namespace std;
int main(){
    long long a;
    cin>>a;
    cout<<(a*(a+1))/2<<"\n";
    cout<<((a*(a+1))/2)*((a*(a+1))/2)<<"\n";
       cout<<((a*(a+1))/2)*((a*(a+1))/2);
}